# Embedded Orchestrate Analysis

The embedded analysis lane owns:

- `MERGED_DRAFT`
- `SCORING_HANDOFF`

It must compare the option set against the acceptance criteria and hand off
facts, contradictions, open questions, and candidate recommendations without
performing the final merge.
