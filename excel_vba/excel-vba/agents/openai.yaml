interface:
  display_name: "Excel VBA Automation"
  short_description: "VBA-first Excel automation behind the contract-ops risk gate"
  default_prompt: "Use $excel-vba only after $excel-xlsm-contract-ops classifies the workbook, then patch workbook logic safely in place, preserve contract-sensitive surfaces, and validate compile, bindings, save-close-reopen, and workbook-qualified macro runs."

policy:
  allow_implicit_invocation: true
