# Repo-local Codex configuration for guarded Excel contract work.

approval_policy = "untrusted"
sandbox_mode = "workspace-write"
allow_login_shell = false
web_search = "live"

[sandbox_workspace_write]
network_access = true

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
